# 🎖️ AI军团（AI Legion）— 企业AI改革加速引擎

> **一句话定位**：从"衡水模式"走出来的个人 AI 军团，让 90% 的执行工作由 AI 完成，你只需做战略决策。

---

## 一、产品概述

AI军团是一套面向中小微企业主的**AI 数字化运营指挥系统**，将情报监控、数据分析、内容创作、任务调度、店铺诊断五大能力整合为一个"虚拟军团"，以可交互的指挥中心面板统一调度。系统内置 **12 种行业基因库**，可对店铺进行自动化诊断与分析，输出可直接落地的运营建议。

### 产品愿景
| 维度 | 说明 |
|------|------|
| 目标用户 | 中小微企业主 / 个体经营者 / 门店老板 |
| 核心价值 | 把"调研—分析—创作—执行"全链路交给 AI |
| 独特风格 | 衡水模式：量化一切 · 直击要害 · 战场隐喻 · 12字诀 |
| 交付形态 | 本地部署 Web 应用（FastAPI 单服务 + 控制面板） |

---

## 二、核心功能

### 1. 指挥中心控制面板 🎛️
- 单页应用（HTML/JS/CSS，无构建依赖），浏览器直接访问
- 实时展示军团状态：战士数量、行业基因库、知识库文档、调度任务
- **店铺管理三步流程**：
  1. 选择行业类型（12 种基因库 + 自定义）
  2. 输入地址 → **实时联想候选**（高德 + 腾讯 + 百度三源 POI）
  3. 选择地址后自动确定 GCJ-02 坐标 → **自动识别行业** → **自动生成诊断分析**

### 2. 情报监控机器人（侦察兵）📡
- 7×24 自动爬取行业资讯与竞品动态
- DeepSeek 生成结构化情报简报
- 企业微信 / 文件多通道推送

### 3. 数据分析 Agent（参谋部）📊
- 支持 CSV / 业务数据问答式分析
- 自动生成 HTML 可视化报告
- 根因分析 + 可执行建议输出

### 4. 内容创作流水线（特种部队）✍️
- 长文 / 短视频脚本 / 朋友圈批量生成
- 内置"衡水人设"品牌语调库（量化表述、强行动号召）

### 5. 自动化工作流（后勤兵）⚙️
- 定时任务调度（Celery Beat 风格 cron）
- 每日情报简报（daily_briefing）、健康检查等内置任务

### 6. 知识库中枢（ChromaDB + RAG）🧠
- 向量化存储 + 语义检索
- 多格式文档知识管理，支撑店铺诊断的行业背景推理

---

## 三、行业基因库（12 种）

| 类型 ID | 行业 | 说明 |
|---------|------|------|
| restaurant | 餐饮 | 菜品、客流、外卖、翻台 |
| retail | 零售 | 商品、坪效、复购 |
| education | 教育培训 | 招生、续费、口碑 |
| healthcare | 健康养生 | 到店、疗程、会员 |
| hotel | 酒店民宿 | 入住率、OTA、点评 |
| real_estate | 房产中介 | 房源、带看、转化 |
| fitness | 健身运动 | 会籍、私教、续卡 |
| florist | 花店 | 花材、节庆、配送 |
| entertainment | 休闲娱乐 | 项目、时段、充值 |
| auto | 汽车服务 | 维保、会员、客单 |
| service | 生活服务 | 预约、技师、口碑 |
| custom | 自定义店铺 | 自由配置诊断维度 |

> 每个基因库为 YAML 配置，定义行业指标、诊断维度与话术模板，支持随时扩展。

---

## 四、技术架构

```
┌─────────────────────────────────────────────┐
│   第一层：指挥中心（你）                       │
│   定战略 · 分任务 · 判质量 · 做决策            │
├───────┬────────┬────────┬─────────┬─────────┤
│ 侦察兵 │ 参谋部 │ 特种部队│ 后勤兵  │  中枢   │
│ 情报监控│ 数据分析│ 内容创作│ 任务调度 │ 知识库 │
│ 爬虫   │ Pandas │DeepSeek│ Celery  │ChromaDB │
│DeepSeek│DeepSeek│ 批量产出│ n8n     │ RAG引擎 │
│ 企微推送│HTML报告│        │ 邮件/DB │ 语义问答│
└───────┴────────┴────────┴─────────┴─────────┘
```

| 层级 | 技术选型 |
|------|----------|
| 后端 | FastAPI + Python 3.10+ |
| AI 引擎 | DeepSeek API（OpenAI 兼容） |
| 地图服务 | 高德（主）+ 腾讯（备）+ 百度（新接入，BD-09→GCJ-02 自动转换） |
| 数据库 | ChromaDB（向量）+ SQLite（业务）+ Redis（可选队列） |
| 任务调度 | Celery + Celery Beat |
| 容器化 | Docker + Docker Compose |

---

## 五、快速启动

### 方式一：Windows 一键启动（推荐）
双击项目根目录 **`start_server.bat`** → 弹出黑色控制台窗口（勿关闭）→ 浏览器访问：

```
http://localhost:8080/
```

### 方式二：手动启动
```bash
cd ai_army
pip install -r requirements.txt
python main.py
# 打开 http://localhost:8080
```

### 配置说明
首次使用需在 `ai_army/.env` 中配置：
- `DEEPSEEK_API_KEY` — DeepSeek 密钥（AI 分析必需）
- `BAIDU_MAP_AK` — 百度地图密钥（地址联想/定位可选，已有高德/腾讯兜底）
- 参考 `.env.example` 模板

---

## 六、目录结构

```
AI军团/
├── start_server.bat        # Windows 一键启动脚本
├── ai_army/
│   ├── main.py              # FastAPI 入口（路由 + 店铺诊断 + 状态接口）
│   ├── requirements.txt     # Python 依赖
│   ├── Dockerfile           # 容器化配置
│   ├── docker-compose.yml   # 编排（含 Redis/Celery）
│   ├── config/
│   │   ├── env.py           # 环境变量管理
│   │   ├── store_templates.py
│   │   ├── industry_genome/ # 12 种行业基因库（YAML）
│   │   └── prompts/         # Prompt 模板 + 衡水语调库
│   ├── src/
│   │   ├── command/         # 指挥中心（控制面板 + 任务分发）
│   │   ├── scouts/          # 侦察兵（爬虫/摘要/推送）
│   │   ├── staff/           # 参谋部（数据分析/报告）
│   │   ├── special_forces/  # 特种部队（内容创作/代码生成）
│   │   ├── logistics/       # 后勤兵（调度/自动化）
│   │   ├── shop/            # 店铺模块（地理编码 geo.py / 诊断）
│   │   ├── knowledge/       # 知识库（ChromaDB / RAG）
│   │   └── routers/         # API 路由层
│   ├── static/              # 前端构建产物
│   └── data/                # 【运行时生成】向量库/店铺配置/缓存
└── 产品总结文档.md
```

---

## 七、部署指南

### Docker 一键部署
```bash
docker-compose up -d            # 完整启动
docker-compose up -d command-center redis   # 仅 Web 面板
docker-compose logs -f command-center       # 查看日志
```

### 腾讯云 Lighthouse / VPS
```bash
scp -r ai_army/ root@your-server:/opt/
cd /opt/ai_army && docker-compose up -d
```

---

## 八、本版本亮点（v1.0 交付重点）

1. **店铺管理全流程打通**：地址输入实时联想 → 三源地图选点 → 坐标精确定位（BD-09/GCJ-02 自动转换）→ 行业自动识别 → 一键 AI 诊断
2. **行业基因库 12 类全覆盖**：YAML 驱动，新增行业零代码
3. **控制面板升级**：运营面板显示店铺定义，支持随时"更换店铺类型"
4. **知识库 + RAG**：行业文档向量化，诊断结果更有依据
5. **稳定运行**：提供独立控制台启动脚本，服务不随会话退出

---

## 九、许可

MIT License — 个人创业者自由使用

**🎯 现在就开始：** 双击 `start_server.bat` → 打开 `http://localhost:8080` → 进入「店铺管理」体验全自动诊断。
